apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: spicyhunt-backend-ingress
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - spicyhunt-api.ops1.websiteserverhost.biz
      secretName: spicyhunt-backend-tls
  rules:
    - host: spicyhunt-api.ops1.websiteserverhost.biz
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: spicyhunt-backend-service
                port:
                  number: 80
